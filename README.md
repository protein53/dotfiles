# dotfiles
All of my dot files
These dot files are intended to be 'installed' using 'stow'
1. git clone https://github.com/protein53/dotfiles ~/dotfiles
2. cd ~/dotfiles
3. ./prestow    --> save orig dot files & prevent stow errors
4. stow *       --> these dot files are now deployed
   

